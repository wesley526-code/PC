<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PC Image Viva | Components Catalog</title>
    
    <!-- 浏览器标签页小图标 -->
    <link rel="icon" type="image/png" href="https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?auto=format&fit=crop&w=100&q=80" />

    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=Noto+Sans:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Noto Sans', sans-serif; background-color: #0b0f19; color: #f3f4f6; }
        .font-orbitron { font-family: 'Orbitron', sans-serif; }
        .glass-card { background: rgba(17, 24, 39, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(59, 130, 246, 0.2); }
        .scrollbar-none::-webkit-scrollbar { display: none; }
        .scrollbar-none { -ms-overflow-style: none; scrollbar-width: none; }
        /* 强制全站商品名称、品牌、分类全部以大写整齐显示 */
        .uniform-text { text-transform: uppercase; }
        /* 拖拽相关样式 */
        .draggable-item { cursor: grab; }
        .draggable-item:active { cursor: grabbing; }
        .dragging { opacity: 0.4; border: 2px dashed #3b82f6; }
    </style>
</head>
<body class="min-h-screen flex flex-col">
    <!-- Top Navigation -->
    <header class="glass-card sticky top-0 z-50 border-b border-blue-900/40 px-4 py-3 flex justify-between items-center">
        <div onclick="goHome()" class="flex items-center space-x-2 cursor-pointer group">
            <img src="https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?auto=format&fit=crop&w=100&q=80" alt="Logo" class="w-9 h-9 rounded-lg object-cover border border-blue-500/40 group-hover:border-blue-400 transition">
            <div>
                <span class="text-sm font-bold font-orbitron text-white block leading-none group-hover:text-blue-400 transition">PC Image VIVA</span>
                <span class="text-[10px] text-blue-400 tracking-wider uppercase">COMPONENTS CATALOG</span>
            </div>
        </div>
        <button onclick="handleAdminClick()" id="admin-btn" class="text-xs bg-slate-800 hover:bg-slate-700 text-slate-300 px-3 py-1.5 rounded-lg border border-slate-700 transition">Admin Panel</button>
    </header>

    <!-- Main Content -->
    <main class="flex-grow max-w-6xl w-full mx-auto p-4">
        <!-- Customer View -->
        <div id="customer-view">
            <div class="mb-6 text-center py-6 bg-gradient-to-r from-blue-900/20 via-slate-900/40 to-blue-900/20 rounded-2xl border border-blue-500/10">
                <h1 class="text-2xl md:text-3xl font-bold font-orbitron text-white mb-2">PC Components Showcase</h1>
                <p class="text-sm text-slate-400">Explore high-performance hardware, check specifications, and inquire via WhatsApp</p>
            </div>

            <!-- Search Bar, Sort & Filters Section -->
            <div class="mb-4 space-y-3">
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-2">
                    <div class="relative sm:col-span-2">
                        <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 pointer-events-none text-slate-400">🔍</span>
                        <input type="text" id="search-input" oninput="handleSearch()" placeholder="Search components by name, brand, specs..." class="w-full bg-slate-900/90 border border-slate-800 rounded-xl pl-10 pr-4 py-2.5 text-sm text-white placeholder-slate-500 focus:outline-none focus:border-blue-500 transition">
                    </div>
                    <div>
                        <select id="sort-select" onchange="handleSort()" class="w-full bg-slate-900/90 border border-slate-800 rounded-xl px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition">
                            <option value="name-asc">Name: A to Z</option>
                            <option value="price-low">Price: Low to High</option>
                            <option value="price-high">Price: High to Low</option>
                        </select>
                    </div>
                </div>

                <!-- Main Category Filters (改为主分类多行自动换行) -->
                <div class="flex flex-wrap gap-2 pb-1" id="category-filters">
                    <button onclick="filterCategory('all')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-blue-600 text-white transition">All Items</button>
                    <button onclick="filterCategory('CPU')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">CPU</button>
                    <button onclick="filterCategory('GPU')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Graphics Card (GPU)</button>
                    <button onclick="filterCategory('Motherboard')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Motherboard</button>
                    <button onclick="filterCategory('RAM')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Memory (RAM)</button>
                    <button onclick="filterCategory('Storage')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">SSD / Storage</button>
                    <button onclick="filterCategory('PSU')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Power Supply (PSU)</button>
                    <button onclick="filterCategory('Cooler')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Cooler</button>
                    <button onclick="filterCategory('Casing')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">PC Casing</button>
                    <button onclick="filterCategory('Accessories')" class="category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Accessories</button>
                </div>

                <!-- Brand / Subcategory Dynamic Bar (改为自动换行，禁止左右滑动) -->
                <div id="filterbar-container" class="hidden space-y-2.5 pt-3 border-t border-slate-800/80">
                    <div id="brand-container" class="hidden flex flex-wrap gap-2 items-center">
                        <span class="text-xs text-amber-400 font-bold uppercase tracking-wider mr-2">Brand:</span>
                        <div id="brand-buttons" class="flex flex-wrap gap-2"></div>
                    </div>
                    <div id="subcategory-container" class="hidden flex flex-wrap gap-2 items-center">
                        <span class="text-xs text-blue-400 font-bold uppercase tracking-wider mr-2">Series:</span>
                        <div id="subcategory-buttons" class="flex flex-wrap gap-2"></div>
                    </div>
                </div>
            </div>

            <!-- Product Grid -->
            <div id="product-grid" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4"></div>
        </div>

        <!-- Admin View -->
        <div id="admin-view" class="hidden">
            <div class="flex justify-between items-center mb-6">
                <div>
                    <h2 class="text-xl font-bold text-white">Manage Components</h2>
                    <p class="text-xs text-slate-400 mt-0.5">Tip: Drag and drop items using the grab handle (⠿) to change their display order.</p>
                </div>
                <div class="space-x-2">
                    <button onclick="showQRCodeModal()" class="bg-indigo-600 hover:bg-indigo-500 text-white text-xs px-3 py-2 rounded-lg transition">Generate QR Code</button>
                    <button onclick="openProductModal()" class="bg-blue-600 hover:bg-blue-500 text-white text-xs px-3 py-2 rounded-lg transition">+ Add New Item</button>
                </div>
            </div>

            <!-- 管理员后台的分类与子分类筛选栏 -->
            <div class="mb-4 glass-card p-3 rounded-xl border border-slate-800 space-y-2">
                <div class="flex flex-wrap gap-2 items-center" id="admin-category-filters">
                    <span class="text-xs text-slate-400 font-medium mr-2">Main Category:</span>
                    <button onclick="adminFilterCategory('all')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-blue-600 text-white transition">All</button>
                    <button onclick="adminFilterCategory('CPU')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">CPU</button>
                    <button onclick="adminFilterCategory('GPU')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">GPU</button>
                    <button onclick="adminFilterCategory('Motherboard')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Motherboard</button>
                    <button onclick="adminFilterCategory('RAM')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">RAM</button>
                    <button onclick="adminFilterCategory('Storage')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Storage</button>
                    <button onclick="adminFilterCategory('PSU')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">PSU</button>
                    <button onclick="adminFilterCategory('Cooler')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Cooler</button>
                    <button onclick="adminFilterCategory('Casing')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Casing</button>
                    <button onclick="adminFilterCategory('Accessories')" class="admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition">Accessories</button>
                </div>
                <!-- 后台子分类筛选 -->
                <div id="admin-subcat-bar" class="hidden flex flex-wrap gap-2 items-center pt-2 border-t border-slate-800">
                    <span class="text-xs text-blue-400 font-medium mr-2">Sub-Category / Series:</span>
                    <div id="admin-subcat-buttons" class="flex flex-wrap gap-2"></div>
                </div>
            </div>

            <div id="admin-product-list" class="space-y-3"></div>
        </div>
    </main>

    <!-- Password Prompt Modal -->
    <div id="password-modal" class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
        <div class="glass-card w-full max-w-sm rounded-2xl p-6 border border-slate-700 text-center relative">
            <button onclick="closePasswordModal()" class="absolute top-4 right-4 text-slate-400 hover:text-white text-lg">✕</button>
            <h3 class="text-lg font-bold text-white mb-2">Admin Authentication</h3>
            <p class="text-xs text-slate-400 mb-4">Please enter your password to access the admin panel</p>
            <form onsubmit="verifyPassword(event)" class="space-y-3">
                <input type="password" id="admin-password-input" placeholder="Enter password" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white text-center focus:outline-none focus:border-blue-500 text-sm">
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-medium py-2 rounded-lg transition text-xs">Login</button>
            </form>
        </div>
    </div>

    <!-- Product Detail Modal -->
    <div id="detail-modal" class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
        <div class="glass-card w-full max-w-lg rounded-2xl p-6 border border-slate-700 max-h-[90vh] overflow-y-auto relative">
            <button onclick="closeDetailModal()" class="absolute top-4 right-4 text-slate-400 hover:text-white text-lg">✕</button>
            <div id="modal-content"></div>
        </div>
    </div>

    <!-- Add/Edit Product Modal -->
    <div id="product-modal" class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
        <div class="glass-card w-full max-w-md rounded-2xl p-6 border border-slate-700 max-h-[90vh] overflow-y-auto relative">
            <button onclick="closeProductModal()" class="absolute top-4 right-4 text-slate-400 hover:text-white text-lg">✕</button>
            <h3 id="modal-title" class="text-lg font-bold text-white mb-4">Add New Component</h3>
            <form id="product-form" onsubmit="saveProduct(event)" class="space-y-3 text-sm">
                <input type="hidden" id="product-id">
                <div>
                    <label class="block text-slate-400 mb-1">Item Name</label>
                    <input type="text" id="p-name" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                </div>
                <div class="grid grid-cols-2 gap-2">
                    <div>
                        <label class="block text-slate-400 mb-1">Normal Price (RM)</label>
                        <input type="number" id="p-price" step="0.01" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                    <div>
                        <label class="block text-slate-400 mb-1">Member Price (RM)</label>
                        <input type="number" id="p-member-price" step="0.01" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Main Category</label>
                    <select id="p-category" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                        <option value="CPU">CPU</option>
                        <option value="GPU">Graphics Card (GPU)</option>
                        <option value="Motherboard">Motherboard</option>
                        <option value="RAM">Memory (RAM)</option>
                        <option value="Storage">SSD / Storage</option>
                        <option value="PSU">Power Supply (PSU)</option>
                        <option value="Cooler">Cooler</option>
                        <option value="Casing">PC Casing</option>
                        <option value="Accessories">Accessories</option>
                    </select>
                </div>
                <div class="grid grid-cols-2 gap-2">
                    <div>
                        <label class="block text-slate-400 mb-1">Brand (Optional)</label>
                        <input type="text" id="p-brand" placeholder="e.g. ASUS, Corsair" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                    <div>
                        <label class="block text-slate-400 mb-1">Sub-Category / Series (Optional)</label>
                        <input type="text" id="p-subcategory" placeholder="e.g. RTX 50 Series" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Image URL (Optional)</label>
                    <input type="url" id="p-image" placeholder="https://..." class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Official Website Link (Optional)</label>
                    <input type="url" id="p-website" placeholder="https://..." class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                </div>
                <div class="grid grid-cols-2 gap-2">
                    <div>
                        <label class="block text-slate-400 mb-1">Spec 1 (Optional)</label>
                        <input type="text" id="p-spec1" placeholder="e.g. 16GB" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                    <div>
                        <label class="block text-slate-400 mb-1">Spec 2 (Optional)</label>
                        <input type="text" id="p-spec2" placeholder="e.g. 850W" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500">
                    </div>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Description / Key Features (Optional)</label>
                    <textarea id="p-desc" rows="3" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500"></textarea>
                </div>
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-medium py-2.5 rounded-lg transition mt-2">Save Item</button>
            </form>
        </div>
    </div>

    <!-- QR Code Modal -->
    <div id="qrcode-modal" class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4">
        <div class="glass-card w-full max-w-sm rounded-2xl p-6 border border-slate-700 text-center relative">
            <button onclick="closeQRCodeModal()" class="absolute top-4 right-4 text-slate-400 hover:text-white text-lg">✕</button>
            <h3 class="text-lg font-bold text-white mb-2">Store QR Code</h3>
            <p class="text-xs text-slate-400 mb-4">Let customers scan this code to view your catalog on their phones</p>
            <div id="qrcode-container" class="bg-white p-4 rounded-xl inline-block mb-4"></div>
            <p id="current-url-text" class="text-xs text-blue-400 break-all bg-slate-900 p-2 rounded border border-slate-800"></p>
        </div>
    </div>

    <script>
        const ADMIN_PASSWORD = "123456";
        const WHATSAPP_NUMBER = "60123456789";

        // 你的 JSONBin.io 凭证
        const JSONBIN_BIN_ID = "6ab4109cac6210605aede323";       
        const JSONBIN_MASTER_KEY = "$2a$10$zeBH9B99dbyUKoeoZ9LgeuST8Z72umn8WZWziMvrJ.Qed4kFkcmnC"; 

        let products = [
            { id: '1', name: 'NVIDIA GeForce RTX 5080', price: 4599.00, memberPrice: 4449.00, category: 'GPU', brand: 'NVIDIA', subcategory: 'RTX 50 Series', image: 'https://images.unsplash.com/photo-1587202372775-e229f172b9d7?auto=format&fit=crop&w=600&q=80', website: 'https://www.nvidia.com', spec1: '16GB GDDR7', spec2: 'PCIe 5.0', desc: 'Next-gen extreme gaming performance with advanced ray tracing and DLSS 4.' },
            { id: '2', name: 'Intel Core i7-14700K', price: 1899.00, memberPrice: 1799.00, category: 'CPU', brand: 'Intel', subcategory: 'Core i7', image: 'https://images.unsplash.com/photo-1591799264318-7e6ef8ddb7ea?auto=format&fit=crop&w=600&q=80', website: 'https://www.intel.com', spec1: '20 Cores / 28 Threads', spec2: 'LGA1700', desc: 'High performance desktop processor for gaming and content creation.' },
            { id: '3', name: 'ASUS ROG MAXIMUS Z890 HERO', price: 2999.00, memberPrice: 2899.00, category: 'Motherboard', brand: 'ASUS', subcategory: 'Intel Z890', image: 'https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=600&q=80', website: 'https://rog.asus.com', spec1: 'LGA1851 / Z890', spec2: 'DDR5 Support', desc: 'Premium enthusiast motherboard with robust power delivery and integrated OLED display.' },
            { id: '4', name: 'Corsair Vengeance RGB 32GB', price: 599.00, memberPrice: 559.00, category: 'RAM', brand: 'Corsair', subcategory: 'DDR5', image: 'https://images.unsplash.com/photo-1562976540-1502c2145c86?auto=format&fit=crop&w=600&q=80', website: 'https://www.corsair.com', spec1: '32GB (2 x 16GB)', spec2: 'DDR5 6000MHz', desc: 'Dynamic multi-zone RGB lighting with high frequency speed.' },
            { id: '5', name: 'HYTE Y70 Touch Panorama Case', price: 799.00, memberPrice: 749.00, category: 'Casing', brand: 'HYTE', subcategory: 'Panoramic', image: 'https://images.unsplash.com/photo-1555680202-c86f0e12f086?auto=format&fit=crop&w=600&q=80', website: 'https://hyte.com', spec1: 'E-ATX / ATX', spec2: 'Built-in 4K Touch Screen', desc: 'Panoramic fish tank design with an integrated luxury touchscreen display.' }
        ];

        let currentCategory = 'all';
        let currentBrand = 'all';
        let currentSubcategory = 'all';
        let currentSort = 'custom';
        let searchQuery = '';
        let isAdmin = false;
        let adminCategoryFilter = 'all';
        let adminSubcategoryFilter = 'all';

        async function fetchProductsFromCloud() {
            try {
                const response = await fetch(`https://api.jsonbin.io/v3/b/${JSONBIN_BIN_ID}/latest`, {
                    headers: { 'X-Master-Key': JSONBIN_MASTER_KEY }
                });
                const result = await response.json();
                if (result && result.record && Array.isArray(result.record.products)) {
                    products = result.record.products;
                }
            } catch (error) {
                console.error("Failed to load from cloud:", error);
            }
            updateFilterBars();
            renderProducts();
        }

        async function saveProductsToCloud() {
            try {
                await fetch(`https://api.jsonbin.io/v3/b/${JSONBIN_BIN_ID}`, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json',
                        'X-Master-Key': JSONBIN_MASTER_KEY
                    },
                    body: JSON.stringify({ products: products })
                });
            } catch (error) {
                console.error("Failed to save to cloud:", error);
            }
        }

        function goHome() {
            if (isAdmin) {
                toggleAdmin(false);
            }
            document.getElementById('search-input').value = '';
            document.getElementById('sort-select').value = 'custom';
            searchQuery = '';
            currentSort = 'custom';
            filterCategory('all');
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        function handleSearch() {
            searchQuery = document.getElementById('search-input').value.toLowerCase().trim();
            renderProducts();
        }

        function handleSort() {
            currentSort = document.getElementById('sort-select').value;
            renderProducts();
        }

        function filterCategory(cat) {
            currentCategory = cat;
            currentBrand = 'all';
            currentSubcategory = 'all'; 
            
            document.querySelectorAll('.category-btn').forEach(btn => {
                if(btn.textContent.toLowerCase().includes(cat === 'all' ? 'all' : cat.toLowerCase())) {
                    btn.className = "category-btn px-4 py-2 rounded-full text-xs font-medium bg-blue-600 text-white transition";
                } else {
                    btn.className = "category-btn px-4 py-2 rounded-full text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition";
                }
            });

            updateFilterBars();
            renderProducts();
        }

        function adminFilterCategory(cat) {
            adminCategoryFilter = cat;
            adminSubcategoryFilter = 'all'; // 切换主分类时重置子分类

            document.querySelectorAll('.admin-cat-btn').forEach(btn => {
                if(btn.textContent.toLowerCase().includes(cat.toLowerCase())) {
                    btn.className = "admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-blue-600 text-white transition";
                } else {
                    btn.className = "admin-cat-btn px-3 py-1.5 rounded-lg text-xs font-medium bg-slate-800 text-slate-300 hover:bg-slate-700 transition";
                }
            });
            updateAdminSubcategoryBar();
            renderProducts();
        }

        function adminFilterSubcategory(sub) {
            adminSubcategoryFilter = sub;
            updateAdminSubcategoryBar();
            renderProducts();
        }

        function updateAdminSubcategoryBar() {
            const bar = document.getElementById('admin-subcat-bar');
            const buttonsContainer = document.getElementById('admin-subcat-buttons');

            let relevant = products;
            if (adminCategoryFilter !== 'all') {
                relevant = products.filter(p => p.category && p.category.trim().toLowerCase() === adminCategoryFilter.toLowerCase());
            }

            const subMap = new Map();
            relevant.forEach(p => {
                if (p.subcategory && p.subcategory.trim() !== '') {
                    const original = p.subcategory.trim();
                    subMap.set(original.toLowerCase(), original);
                }
            });

            if (subMap.size > 0 && adminCategoryFilter !== 'all') {
                bar.classList.remove('hidden');
                let html = `<button onclick="adminFilterSubcategory('all')" class="px-3 py-1 rounded-md text-xs font-semibold ${adminSubcategoryFilter === 'all' ? 'bg-blue-500/30 text-blue-300 border border-blue-500/50' : 'bg-slate-900 text-slate-300 border border-slate-800'} transition">All Series</button>`;
                subMap.forEach((original, lower) => {
                    const isActive = adminSubcategoryFilter.toLowerCase() === lower;
                    html += `<button onclick="adminFilterSubcategory('${original}')" class="px-3 py-1 rounded-md text-xs font-semibold uppercase ${isActive ? 'bg-blue-500/30 text-blue-300 border border-blue-500/50' : 'bg-slate-900 text-slate-300 border border-slate-800'} transition">${original}</button>`;
                });
                buttonsContainer.innerHTML = html;
            } else {
                bar.classList.add('hidden');
                adminSubcategoryFilter = 'all';
            }
        }

        function updateFilterBars() {
            const filterBarContainer = document.getElementById('filterbar-container');
            const brandContainer = document.getElementById('brand-container');
            const brandButtons = document.getElementById('brand-buttons');
            const subcontainer = document.getElementById('subcategory-container');
            const subButtons = document.getElementById('subcategory-buttons');

            let relevantProducts = products;
            if (currentCategory !== 'all') {
                relevantProducts = products.filter(p => p.category && p.category.trim().toLowerCase() === currentCategory.toLowerCase());
            }

            const brandMap = new Map();
            relevantProducts.forEach(p => {
                if (p.brand && p.brand.trim() !== '') {
                    const originalBrand = p.brand.trim();
                    const lowerBrand = originalBrand.toLowerCase();
                    if (!brandMap.has(lowerBrand)) {
                        brandMap.set(lowerBrand, originalBrand);
                    }
                }
            });

            let subProducts = relevantProducts;
            if (currentBrand !== 'all') {
                subProducts = relevantProducts.filter(p => p.brand && p.brand.trim().toLowerCase() === currentBrand.toLowerCase());
            }
            const subMap = new Map();
            subProducts.forEach(p => {
                if (p.subcategory && p.subcategory.trim() !== '') {
                    const originalSub = p.subcategory.trim();
                    const lowerSub = originalSub.toLowerCase();
                    if (!subMap.has(lowerSub)) {
                        subMap.set(lowerSub, originalSub);
                    }
                }
            });

            let hasFilter = false;

            if (brandMap.size > 0) {
                brandContainer.classList.remove('hidden');
                hasFilter = true;
                let bHtml = `<button onclick="filterBrand('all')" class="px-3 py-1.5 rounded-lg text-xs font-semibold ${currentBrand === 'all' ? 'bg-amber-500/30 text-amber-300 border border-amber-500/50' : 'bg-slate-900 text-slate-300 hover:text-white border border-slate-800'} transition">All Brands</button>`;
                brandMap.forEach((originalBrand, lowerBrand) => {
                    const isActive = currentBrand.toLowerCase() === lowerBrand;
                    bHtml += `<button onclick="filterBrand('${originalBrand}')" class="px-3 py-1.5 rounded-lg text-xs font-semibold uppercase ${isActive ? 'bg-amber-500/30 text-amber-300 border border-amber-500/50' : 'bg-slate-900 text-slate-300 hover:text-white border border-slate-800'} transition">${originalBrand}</button>`;
                });
                brandButtons.innerHTML = bHtml;
            } else {
                brandContainer.classList.add('hidden');
            }

            if (subMap.size > 0 && currentCategory !== 'all') {
                subcontainer.classList.remove('hidden');
                hasFilter = true;
                let sHtml = `<button onclick="filterSubcategory('all')" class="px-3 py-1.5 rounded-lg text-xs font-semibold ${currentSubcategory === 'all' ? 'bg-blue-500/30 text-blue-300 border border-blue-500/50' : 'bg-slate-900 text-slate-300 hover:text-white border border-slate-800'} transition">All Series</button>`;
                subMap.forEach((originalSub, lowerSub) => {
                    const isActive = currentSubcategory.toLowerCase() === lowerSub;
                    sHtml += `<button onclick="filterSubcategory('${originalSub}')" class="px-3 py-1.5 rounded-lg text-xs font-semibold uppercase ${isActive ? 'bg-blue-500/30 text-blue-300 border border-blue-500/50' : 'bg-slate-900 text-slate-300 hover:text-white border border-slate-800'} transition">${originalSub}</button>`;
                });
                subButtons.innerHTML = sHtml;
            } else {
                subcontainer.classList.add('hidden');
            }

            if (hasFilter && currentCategory !== 'all') {
                filterBarContainer.classList.remove('hidden');
            } else {
                filterBarContainer.classList.add('hidden');
            }
        }

        function filterBrand(brand) {
            currentBrand = brand;
            currentSubcategory = 'all'; 
            updateFilterBars();
            renderProducts();
        }

        function filterSubcategory(sub) {
            currentSubcategory = sub;
            updateFilterBars();
            renderProducts();
        }

        function renderProducts() {
            const grid = document.getElementById('product-grid');
            const adminList = document.getElementById('admin-product-list');
            
            // 1. 前台筛选逻辑
            let filtered = products.filter(p => {
                const pCat = p.category ? p.category.trim().toLowerCase() : '';
                const pBrand = p.brand ? p.brand.trim().toLowerCase() : '';
                const pSub = p.subcategory ? p.subcategory.trim().toLowerCase() : '';

                const matchesCategory = (currentCategory === 'all' || pCat === currentCategory.toLowerCase());
                const matchesBrand = (currentBrand === 'all' || pBrand === currentBrand.toLowerCase());
                const matchesSubcategory = (currentSubcategory === 'all' || pSub === currentSubcategory.toLowerCase());
                
                const matchesSearch = !searchQuery || 
                    p.name.toLowerCase().includes(searchQuery) || 
                    pCat.includes(searchQuery) ||
                    pBrand.includes(searchQuery) ||
                    pSub.includes(searchQuery) ||
                    (p.spec1 && p.spec1.toLowerCase().includes(searchQuery)) ||
                    (p.spec2 && p.spec2.toLowerCase().includes(searchQuery)) ||
                    (p.desc && p.desc.toLowerCase().includes(searchQuery));

                return matchesCategory && matchesBrand && matchesSubcategory && matchesSearch;
            });

            // 前台排序
            if (currentSort !== 'custom') {
                filtered.sort((a, b) => {
                    const priceA = a.memberPrice !== undefined ? a.memberPrice : a.price;
                    const priceB = b.memberPrice !== undefined ? b.memberPrice : b.price;

                    if (currentSort === 'price-low') {
                        return priceA - priceB;
                    } else if (currentSort === 'price-high') {
                        return priceB - priceA;
                    } else if (currentSort === 'name-asc') {
                        return a.name.localeCompare(b.name);
                    } else {
                        return 0;
                    }
                });
            }
            
            grid.innerHTML = filtered.map(p => {
                const mPrice = p.memberPrice !== undefined ? p.memberPrice : p.price;
                const imgSrc = p.image ? p.image : 'https://images.unsplash.com/photo-1587202372775-e229f172b9d7?auto=format&fit=crop&w=600&q=80';
                
                let badgesHtml = `<span class="absolute top-2 right-2 bg-blue-600/80 text-white text-[10px] px-2 py-0.5 rounded backdrop-blur uppercase">${p.category || ''}</span>`;
                if (p.brand) {
                    badgesHtml += `<span class="absolute top-2 left-2 bg-amber-600/80 text-white text-[10px] px-2 py-0.5 rounded backdrop-blur uppercase">${p.brand}</span>`;
                } else if (p.subcategory) {
                    badgesHtml += `<span class="absolute top-2 left-2 bg-slate-900/80 text-blue-300 text-[10px] px-2 py-0.5 rounded backdrop-blur border border-blue-500/30 uppercase">${p.subcategory}</span>`;
                }
                
                return `
                <div onclick="openDetail('${p.id}')" class="glass-card rounded-xl overflow-hidden border border-slate-800 hover:border-blue-500/50 transition cursor-pointer group">
                    <div class="h-48 overflow-hidden bg-slate-950/80 relative flex items-center justify-center p-2">
                        <img src="${imgSrc}" alt="${p.name}" class="max-h-full max-w-full object-contain group-hover:scale-105 transition duration-300">
                        ${badgesHtml}
                    </div>
                    <div class="p-4">
                        <h3 class="font-bold text-white text-sm mb-2 truncate uniform-text">${p.name}</h3>
                        <div class="mb-3 bg-slate-900/50 p-2.5 rounded-lg border border-slate-800 flex items-center justify-between">
                            <div>
                                <span class="text-[10px] text-slate-400 block uppercase">Normal</span>
                                <span class="text-xs text-slate-300 font-orbitron">RM ${p.price.toFixed(2)}</span>
                            </div>
                            <div class="text-right">
                                <span class="text-[10px] text-amber-400 font-bold block uppercase">Member Price</span>
                                <span class="text-lg text-amber-400 font-orbitron font-black">RM ${mPrice.toFixed(2)}</span>
                            </div>
                        </div>
                        <div class="text-xs text-slate-400 grid grid-cols-2 gap-1 mb-3">
                            <div>${p.spec1 || ''}</div>
                            <div>${p.spec2 || ''}</div>
                        </div>
                        <button class="w-full bg-slate-800 hover:bg-blue-600 text-slate-200 hover:text-white text-xs py-2 rounded-lg transition font-medium">View Specs / Inquiry</button>
                    </div>
                </div>
            `;
            }).join('') || `<div class="col-span-full text-center py-12 text-slate-500">No components found matching your search or filter</div>`;

            // 2. 后台管理列表（带有拖拽支持）
            let adminFiltered = products;
            if (adminCategoryFilter !== 'all') {
                adminFiltered = adminFiltered.filter(p => p.category && p.category.trim().toLowerCase() === adminCategoryFilter.toLowerCase());
            }
            if (adminSubcategoryFilter !== 'all') {
                adminFiltered = adminFiltered.filter(p => p.subcategory && p.subcategory.trim().toLowerCase() === adminSubcategoryFilter.toLowerCase());
            }

            adminList.innerHTML = adminFiltered.map(p => {
                const mPrice = p.memberPrice !== undefined ? p.memberPrice : p.price;
                return `
                <div draggable="true" 
                     ondragstart="drag(event)" 
                     ondragover="allowDrop(event)" 
                     ondrop="drop(event, '${p.id}')" 
                     data-id="${p.id}"
                     class="draggable-item glass-card p-3 rounded-xl flex items-center justify-between border border-slate-800 hover:border-blue-500/40 transition">
                    <div class="flex items-center space-x-3">
                        <span class="text-slate-500 hover:text-white cursor-grab text-lg select-none px-1" title="Drag to reorder">⠿</span>
                        <img src="${p.image || 'https://images.unsplash.com/photo-1587202372775-e229f172b9d7?auto=format&fit=crop&w=600&q=80'}" class="w-12 h-12 object-cover rounded-lg bg-slate-900">
                        <div>
                            <div class="font-bold text-white text-sm uniform-text">${p.name}</div>
                            <div class="text-xs text-blue-400">Normal: RM ${p.price.toFixed(2)} | <span class="text-amber-400 font-bold">Member: RM ${mPrice.toFixed(2)}</span></div>
                            <div class="text-[11px] text-slate-400 mt-0.5 space-x-1">
                                <span class="bg-blue-900/40 text-blue-300 px-1.5 py-0.5 rounded border border-blue-500/25 uppercase">${p.category || 'N/A'}</span>
                                ${p.brand ? `<span class="bg-amber-900/40 text-amber-300 px-1.5 py-0.5 rounded border border-amber-500/25 uppercase">${p.brand}</span>` : ''}
                                ${p.subcategory ? `<span class="bg-slate-800 text-slate-300 px-1.5 py-0.5 rounded border border-slate-700 uppercase">${p.subcategory}</span>` : ''}
                            </div>
                        </div>
                    </div>
                    <div class="space-x-2">
                        <button onclick="editProduct('${p.id}')" class="bg-slate-800 hover:bg-slate-700 text-slate-300 text-xs px-3 py-1.5 rounded-lg">Edit</button>
                        <button onclick="deleteProduct('${p.id}')" class="bg-red-900/30 hover:bg-red-900/50 text-red-400 text-xs px-3 py-1.5 rounded-lg border border-red-800/30">Delete</button>
                    </div>
                </div>
            `;
            }).join('') || `<div class="text-center py-6 text-slate-500 text-xs">No items in this category</div>`;
        }

        // --- 拖拽排序逻辑 ---
        let draggedId = null;

        function drag(e) {
            draggedId = e.target.getAttribute('data-id');
            e.dataTransfer.setData('text/plain', draggedId);
            e.target.classList.add('dragging');
        }

        function allowDrop(e) {
            e.preventDefault();
        }

        async function drop(e, targetId) {
            e.preventDefault();
            if (!draggedId || draggedId === targetId) return;

            const fromIndex = products.findIndex(p => p.id === draggedId);
            const toIndex = products.findIndex(p => p.id === targetId);

            if (fromIndex !== -1 && toIndex !== -1) {
                // 调整数组顺序
                const movedItem = products.splice(fromIndex, 1)[0];
                products.splice(toIndex, 0, movedItem);

                // 自动保存至云端
                await saveProductsToCloud();
                updateFilterBars();
                renderProducts();
            }
            draggedId = null;
        }

        function handleAdminClick() {
            if (isAdmin) {
                toggleAdmin(false);
            } else {
                document.getElementById('password-modal').classList.remove('hidden');
                document.getElementById('admin-password-input').value = '';
                document.getElementById('admin-password-input').focus();
            }
        }

        function verifyPassword(e) {
            e.preventDefault();
            const inputPwd = document.getElementById('admin-password-input').value;
            if (inputPwd === ADMIN_PASSWORD) {
                closePasswordModal();
                toggleAdmin(true);
            } else {
                alert('Incorrect password!');
            }
        }

        function closePasswordModal() {
            document.getElementById('password-modal').classList.add('hidden');
        }

        function toggleAdmin(state) {
            isAdmin = state;
            document.getElementById('customer-view').classList.toggle('hidden', isAdmin);
            document.getElementById('admin-view').classList.toggle('hidden', !isAdmin);
            document.getElementById('admin-btn').textContent = isAdmin ? 'Exit Admin' : 'Admin Panel';
            if(isAdmin) {
                updateAdminSubcategoryBar();
                renderProducts();
            }
        }

        function openDetail(id) {
            const p = products.find(item => item.id === id);
            if(!p) return;
            const mPrice = p.memberPrice !== undefined ? p.memberPrice : p.price;
            const waText = encodeURIComponent(`Hello, I am interested in this component: ${p.name} (Normal: RM ${p.price} | Member: RM ${mPrice})`);
            
            const imgSrc = p.image ? p.image : 'https://images.unsplash.com/photo-1587202372775-e229f172b9d7?auto=format&fit=crop&w=600&q=80';
            const websiteBtnHtml = p.website ? `
                <a href="${p.website}" target="_blank" class="block w-full bg-slate-800 hover:bg-slate-700 text-blue-400 hover:text-blue-300 text-center font-medium py-2 rounded-xl transition text-xs border border-blue-500/30 mb-2">🌐 Visit Official Website</a>
            ` : '';

            const specsHtml = (p.spec1 || p.spec2) ? `
                <div class="bg-slate-900/60 rounded-xl p-3 border border-slate-800 text-xs space-y-2 mb-4">
                    ${p.spec1 ? `<div class="flex justify-between"><span class="text-slate-400">Primary Specification:</span> <span class="text-white">${p.spec1}</span></div>` : ''}
                    ${p.spec2 ? `<div class="flex justify-between"><span class="text-slate-400">Secondary Specification:</span> <span class="text-white">${p.spec2}</span></div>` : ''}
                </div>
            ` : '';

            const descHtml = p.desc ? `<p class="text-xs text-slate-300 mb-4 leading-relaxed">${p.desc}</p>` : '';

            document.getElementById('modal-content').innerHTML = `
                <div class="h-64 rounded-xl overflow-hidden bg-slate-950/80 mb-4 flex items-center justify-center p-2 relative">
                    <img src="${imgSrc}" class="max-h-full max-w-full object-contain">
                    ${p.brand ? `<span class="absolute top-2 left-2 bg-amber-600/80 text-white text-[10px] px-2 py-0.5 rounded backdrop-blur uppercase">${p.brand}</span>` : ''}
                </div>
                <div class="flex justify-between items-start mb-3">
                    <h2 class="text-lg font-bold text-white uniform-text">${p.name}</h2>
                    <span class="text-xs bg-blue-600/30 text-blue-300 px-2 py-0.5 rounded border border-blue-500/30 uppercase">${p.category || ''}</span>
                </div>
                
                <div class="grid grid-cols-2 gap-3 mb-4 bg-slate-900/80 p-3.5 rounded-xl border border-amber-500/30">
                    <div>
                        <div class="text-[10px] text-slate-400 uppercase">Standard Price</div>
                        <div class="text-base font-orbitron text-slate-300">RM ${p.price.toFixed(2)}</div>
                    </div>
                    <div>
                        <div class="text-[10px] text-amber-400 uppercase font-bold">Member Price</div>
                        <div class="text-xl font-orbitron font-black text-amber-400">RM ${mPrice.toFixed(2)}</div>
                    </div>
                </div>

                ${specsHtml}
                ${descHtml}
                
                ${websiteBtnHtml}
                <a href="https://wa.me/${WHATSAPP_NUMBER}?text=${waText}" target="_blank" class="block w-full bg-emerald-600 hover:bg-emerald-500 text-white text-center font-medium py-2.5 rounded-xl transition text-sm">Inquiry via WhatsApp</a>
            `;
            document.getElementById('detail-modal').classList.remove('hidden');
        }

        function closeDetailModal() { document.getElementById('detail-modal').classList.add('hidden'); }
        function openProductModal() {
            document.getElementById('modal-title').textContent = "Add New Component";
            document.getElementById('product-form').reset();
            document.getElementById('product-id').value = '';
            document.getElementById('product-modal').classList.remove('hidden');
        }
        function closeProductModal() { document.getElementById('product-modal').classList.add('hidden'); }

        async function saveProduct(e) {
            e.preventDefault();
            const id = document.getElementById('product-id').value;
            const data = {
                id: id || Date.now().toString(),
                name: document.getElementById('p-name').value.trim(),
                price: parseFloat(document.getElementById('p-price').value),
                memberPrice: parseFloat(document.getElementById('p-member-price').value),
                category: document.getElementById('p-category').value,
                brand: document.getElementById('p-brand').value.trim(),
                subcategory: document.getElementById('p-subcategory').value.trim(),
                image: document.getElementById('p-image').value.trim(),
                website: document.getElementById('p-website').value.trim(),
                spec1: document.getElementById('p-spec1').value.trim(),
                spec2: document.getElementById('p-spec2').value.trim(),
                desc: document.getElementById('p-desc').value.trim()
            };
            if(id) {
                const idx = products.findIndex(p => p.id === id);
                if(idx !== -1) products[idx] = data;
            } else {
                products.unshift(data);
            }
            
            await saveProductsToCloud();
            closeProductModal();
            updateFilterBars();
            updateAdminSubcategoryBar();
            renderProducts();
        }

        function editProduct(id) {
            const p = products.find(item => item.id === id);
            if(!p) return;
            document.getElementById('modal-title').textContent = "Edit Component Details";
            document.getElementById('product-id').value = p.id;
            document.getElementById('p-name').value = p.name || '';
            document.getElementById('p-price').value = p.price || '';
            document.getElementById('p-member-price').value = p.memberPrice !== undefined ? p.memberPrice : '';
            document.getElementById('p-category').value = p.category || 'CPU';
            document.getElementById('p-brand').value = p.brand || '';
            document.getElementById('p-subcategory').value = p.subcategory || '';
            document.getElementById('p-image').value = p.image || '';
            document.getElementById('p-website').value = p.website || '';
            document.getElementById('p-spec1').value = p.spec1 || '';
            document.getElementById('p-spec2').value = p.spec2 || '';
            document.getElementById('p-desc').value = p.desc || '';
            document.getElementById('product-modal').classList.remove('hidden');
        }

        async function deleteProduct(id) {
            if(confirm('Are you sure you want to delete this component?')) {
                products = products.filter(p => p.id !== id);
                await saveProductsToCloud();
                updateFilterBars();
                updateAdminSubcategoryBar();
                renderProducts();
            }
        }

        function showQRCodeModal() {
            const container = document.getElementById('qrcode-container');
            container.innerHTML = '';
            const currentUrl = window.location.href;
            document.getElementById('current-url-text').textContent = currentUrl;
            new QRCode(container, { text: currentUrl, width: 160, height: 160 });
            document.getElementById('qrcode-modal').classList.remove('hidden');
        }

        function closeQRCodeModal() { document.getElementById('qrcode-modal').classList.add('hidden'); }

        fetchProductsFromCloud();
    </script>
</body>
</html>
